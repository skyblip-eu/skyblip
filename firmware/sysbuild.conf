# sysbuild.conf — build MCUboot alongside the app so signed images + A/B swap
# work out of the box (west build --sysbuild). MCUboot occupies mcuboot_pad /
# slot0 / slot1 from the board devicetree partitions.
SB_CONFIG_BOOTLOADER_MCUBOOT=y
